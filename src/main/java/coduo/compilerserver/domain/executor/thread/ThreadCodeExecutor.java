package coduo.compilerserver.domain.executor.thread;

import coduo.compilerserver.domain.Project;
import coduo.compilerserver.domain.executionresult.ExecutionResult;
import coduo.compilerserver.domain.executor.CodeExecutor;

public class ThreadCodeExecutor implements CodeExecutor {

    @Override
    public ExecutionResult execute(Project project, String executionId) {
        return null;
    }
}